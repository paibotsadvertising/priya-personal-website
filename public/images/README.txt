Drop these files here:
  - logo.png        (the PAIBOTS multi-color outline logo, transparent BG, ~1024px wide)
  - logo-mark.png   (just the "P" mark for favicon, ~512x512)
For now the site uses a text-based logo fallback styled to match the brand colors.
